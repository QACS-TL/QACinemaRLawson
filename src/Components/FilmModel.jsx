export default class FilmModel {
    constructor(title, shortname, synopsis, agecert, _id) {
      this.title = title;
      this.shortname = shortname;
      this.synopsis = synopsis;
      this.agecert = agecert;
      this._id = _id;
    }
  }